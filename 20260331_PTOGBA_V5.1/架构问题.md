# V5.1 SystemC 架构问题

**Task ID**: 20260331_PTOGBA_V5.1

**问题描述**: 
在 `sc_main()` 主循环中调用了 `wait()`，但 `sc_main()` 不是 SC_THREAD，这违反了 SystemC 规则。

**错误信息**:
```
Error: (E519) wait() is only allowed in SC_THREADs and SC_CTHREADs
```

**解决方案**:
有两个选择:
1. **方案 A**: 创建一个 `Simulation_Thread` 类（SC_MODULE）来处理主循环
2. **方案 B**: 重新设计架构，使用 sc_fifo 进行任务传递

**建议**: 方案 A 更简单,更符合当前设计

**等待铲屎官指导**:
- 选择 A 还是 B？
- 是否有其他设计建议？

---

## 当前代码状态

- ✅ 编译通过
- ✅ 参数化配置完成
- ✅ SystemC 模块定义完成
- ❌ 主循环中的 wait() 错误

**代码路径**: `/root/shared_memory/hinton/20260331_PTOGBA_V5.1/src/top.cpp`
